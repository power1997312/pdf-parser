# 代码审查报告 (review.md)

> 审查对象：/Users/plankg/PDF识别/src/pdfparser/（11 个模块，约 1500 行）
> 审查方式：编译检查 + 静态检查 + 全样本端到端回归（pytest 9/9 通过）

## 结论

**通过**。无阻塞级（Critical）问题。开发过程中发现并修复的问题见下。

## 开发过程发现并修复的问题（已解决）

| # | 级别 | 问题 | 修复 |
|---|------|------|------|
| 1 | Critical | `extract.py` 方法名与属性名冲突（`_image_infos` 被 None 遮蔽） | 属性改名 `_cached_image_infos` |
| 2 | Critical | rawdict 的 span 无 `text` 键，导致文本全空 | 改为从 `chars[].c` 拼接 |
| 3 | Critical | `TextLine` 缺少 `italic` 字段 | 补字段 |
| 4 | Critical | pdfplumber Row/Column 属性错误（`top`/`x0` 不存在，仅 `bbox`） | 统一改用 `bbox` |
| 5 | Critical | pdfplumber Table 无 `lines` 属性 | flavor 由策略显式传入 |
| 6 | Major | 段合并过度：标题与正文混块、字号用平均值失真 | 字号突变断段 + font_sig 用众数字号 |
| 7 | Major | 标题判定视觉缝过严，大纲 0 条 | 改为"下一块更大字号"软否决 |
| 8 | Major | 图题与正文合并、caption 误判 | 图题行独占一段 + caption 块级限制 |
| 9 | Major | 页眉页脚混入标题子树、页码未识别 | 树构建跳过 + 页码模式识别 |
| 10 | Major | 列表"引导句+多列表区域"无法识别 | 重构为区域拆分（intro/list 交替） |
| 11 | Major | 无线表检测失败（pdfplumber stream 质量差） | 自研全页检测（字段间隙+空白带分段） |
| 12 | Minor | 跨页接续未触发（依赖 header_rows） | 放宽判据：列数+列边界+首行匹配 |
| 13 | Minor | Markdown 渲染漏 `text` 类型块、图片 asset 未回填 | 渲染补分支 + export 后回填 |
| 14 | Minor | 未使用导入（document/extract/lists/order） | 清理 |
| 15 | Critical | 扫描件分支提前 `doc.close()` 与 finally 重复关闭 → `ValueError: document closed` | 移除提前 close，统一由 finally 处理 |

## 真实文档验证（complex_pdfs/）

| 文档 | 结果 |
|------|------|
| SRS_online_exam_system_EN（工程需求文档） | ✅ 56 条标题 |
| SRS_system_analysis_design_EN | ✅ 21 条标题 |
| SDD_template_software_design | ✅ 9 条标题 |
| srs_template_ieee_830 | ✅ 9 条标题 |
| rfc9110_http_semantics（标准文档） | ✅ 143 条标题 |
| 中文期刊（双栏+公式） | 3-6 条标题（双栏学术论文，标题多无编号，属已知权衡） |
| arXiv 论文（双栏） | 0-3 条（无编号标题+公式密集，难度最高） |
| jos_2026_LLM根因分析综述 | ✅ L0 正确判别为扫描型，明确报告"需 OCR" |

---

# 第二轮：complex_pdfs 全量强力验证与优化（2026-08-14）

## 发现并修复的问题（TDD：先写失败测试 test_real_docs.py，再修复）

| # | 级别 | 问题（真实文档暴露） | 修复 |
|---|------|----------------------|------|
| V1 | Critical | 无线表检测把双栏正文/公式/参考文献误判为表格（LLM_Survey 348 个"表"） | 列对齐一致性 + 行字段数一致 + 列数范围[2,12] 三重校验 |
| V2 | Critical | 双栏页面整页文本被聚成巨型表（18 列） | 多栏匹配防线：检测表列数与页面栏数一致且列起点吻合 → 排除 |
| V3 | Major | 页面框线/图表（旋转轴标签）误检为 lattice 表 | n_rows≥2 且 (≥3 行或 ≥3 列) + 单元格非空率≥25% + **旋转词占比>30% 判图表排除** |
| V4 | Major | 双栏/中文标题漏检：LaTeX 章节标题比正文小（CMBX9 vs CMR9） | 候选字号下限放宽到 body-2.0 + 全大写加粗判据 + 编号模式扩展（一、/（一）） |
| V5 | Major | 中文标题用不同字体族（楷体 FZKTK/黑体 FZHTK 同字号正文） | 候选判据含"字体族不同" + 同层合并映射修复 + 智能字体断段（短行+字体变+行距） |
| V6 | Major | CMR10 高频字体被当标题候选（body 误判为 CMR9） | 相对频率门槛 freq_limit = body_cnt*0.5 + 字号≥body+1.5 无条件保留 |
| V7 | Major | 正文碎片误判标题（'scholarly works.'、'But both the'） | 续行排除 + 连字符结尾排除 + 公式符号排除 + 作者行（∗†‡）排除 |
| V8 | Major | LaTeX bold 标志位缺失（CMBX9 的 flags 无 bold 位） | 字体名推断加粗（cmb/black/bold...） |
| V9 | Major | arXiv 标识/装饰字母误判标题 | arxiv:/doi:/http 前缀排除 + 单字符块不参与软否决 |
| V10 | Major | 1.1 背景（2 汉字标题）被 alpha_n≥3 误杀 | 降为 ≥2 + 公式符号排除防 '1 + Nv' 回归 |
| V11 | Minor | 页眉行与标题行合并导致块级页眉识别失效 | 改为**行级**跨页重复页眉识别 |
| V12 | Minor | 跨页拼接后 SRS 召回 129%（重复计数） | 表格误检修复后自然回落（111.9%） |

## 优化前后对比（复杂文档全量）

| 文档 | 表格数 基线→最终 | 标题数 基线→最终 | 召回 基线→最终 |
|------|-----------------|-----------------|---------------|
| LLM_Survey | 348→24 | 0→3 | 151%→99% |
| AlexNet | 29→1 | 1→13 | 137%→94% |
| RFC 9110 | 87→13 | 143→209 | 101%→96% |
| SRS_online | 31→13 | 56→61 | 129%→112% |
| gov 指南 | 31→1 | 0→37 | 97%→77%* |
| jos_P2P | 26→9 | 6→10 | 97%→90% |
| Attention | 20→8 | 0→12 | 105%→96% |
| ResNet | 33→16 | 3→20 | 115%→101% |
| caict 白皮书 | 6→1 | 3→33 | 99%→97% |
| srs_template | 1→1 | 9→40 | 97%→97% |

*注：gov 77% 为统计口径差异（raw 含换行符 + 图表坐标标签 AA/AC 等），逐行比对实际仅 32 行图例标签未入正文，真实正文无丢失。

## 测试

- 合成样本 9 项 + 真实文档 golden 17 项 = **26/26 通过**
- 量化工具：tools/evaluate.py（批量指标 + baseline/final.json）

## 静态检查残留

- 无（`__init__.py` 的 re-export 为有意设计）。

## 已知行为权衡（文档化，非缺陷）

1. 无编号的文档大标题（如"核电仪控系统验证与确认实施方案"）不判为标题——标题判定要求编号模式，工程文档章节均有编号，符合预期；
2. "6.1 检修周期：每年一次全面检修。" 这类带编号的短正文句可能被判标题（编号+短行+无句号）——规则固有权衡，文档化；
3. 表格引擎当前为 pdfplumber 降级模式（本机无 Ghostscript）；安装 Ghostscript 后自动启用 camelot 提升有线表准确率，无需改代码。

---

# 第三轮：用户验收反馈驱动的修复（2026-08-15）

用户检查正式运行输出后反馈三类问题，全部按 TDD 修复（新增 tests 断言 4 项，30/30 通过）：

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| W1 | gov 段落被拆成多行（原 PDF 连贯） | 中文公文"首行缩进 2 字符"(~30pt) 超过段合并 x0 容差 12pt | x0 容差 12→32pt + 渲染层段内换行合并（_join_lines：中文紧连/英文补空格） |
| W2 | gov 结构图（体系框架图）内文字被当正文 | 图中短文本标签（AA术语/GA数据基础设施安全）与正文无区分 | 结构图区域检测：短文本块(2-10字符)+≥4行带+中文占比≥50% → figure 块；排除编号标题/正文行碎片 |
| W3 | gov 图内并排标签（B 数据基础设施 C 数据资源）被 MuPDF 并成一行 | L1 行聚类按基线合并同 y 标签 | span 间隙拆分试验后回退（正文中英混排误拆），改由结构图检测吸收 |
| W4 | ResNet 表格表头渲染两次 | header_rows=0 时表头行作为 header 渲染后又从 row0 开始数据区 | 渲染始终从 rows[1] 开始数据区 |
| W5 | ResNet 折线图被误判为表格 | pgfplots 旋转轴标签（rorre=error）upright 标志失效 | 图表检测增强：竖排词(≥3字符且高>宽)≥2 即判图表；数字单元格(单字符)不误判 |
| W6 | ResNet 矢量图（tikz）无 figure 块 | 矢量图无位图 xref，get_image_info 检测不到 | 矢量图检测：图题(Figure N/图N)上方矢量线≥15 且无位图 → figure 块 |
| W7 | 长图题（>60 字符）未被识别；"Fig. 6 shows..." 引用句误判图题 | caption 判定长度限制 + 句号排除过严/过松 | 图题首行≤80 + "Figure N shows/illustrates..." 引用句正则排除 |

## 最终验证（2026-08-15）

- 合成 9 + 真实 21 = **30/30 测试通过**
- gov：段落完整连贯、9 个结构图（含全部图内标注）、37 条标题
- ResNet：5 个矢量图 figure、14 个真表格（折线图已拦截）、20 条标题
- 已知边界：LaTeX 数学公式(CID 无 ToUnicode)输出 (cid:xx)；个别分页处段落断行
